# CPU-only PyTorch instalado antes en el Dockerfile

# LLM API
anthropic>=0.40

# Vector store + embeddings CPU
qdrant-client>=1.13
sentence-transformers>=3.3

# UI
streamlit>=1.40
pillow>=11.0

# OCR local — headless opencv (sin X11/display) para ambiente serverless
# rapidocr-onnxruntime se instala con --no-deps en el Dockerfile
opencv-python-headless>=4.5.1.48
onnxruntime>=1.7.0
pyclipper>=1.2.0
Shapely>=1.7.1

# HTTP (llamadas Ollama si disponible)
httpx>=0.27

# Config
python-dotenv>=1.0
pydantic>=2.10
pydantic-settings>=2.7
typer>=0.15
rich>=13.9
tqdm>=4.67
numpy>=1.26
